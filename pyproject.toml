[project]
name = "isthatmynewjob"
version = "0.1.0"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
markers = ["contrato: tests contra APIs reales, ejecución manual"]
addopts = "-m 'not contrato'"
